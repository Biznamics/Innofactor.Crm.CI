{
  "name": "MinifyJS",
  "version": "1.0.0",
  "type": "module",
  "description": "A project that uses gulp to optimize JavaScript code",
  "main": "gulpfile.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "npx npm-check-updates -u && npm i"
  },
  "devDependencies": {
    "gulp": "^5.0.1",
    "gulp-cli": "^3.1.0",
    "gulp-rename": "^2.1.0",
    "gulp-strip-debug": "^7.0.0",
    "gulp-terser": "^2.1.0",
    "plugin-error": "^2.0.1"
  }
}
